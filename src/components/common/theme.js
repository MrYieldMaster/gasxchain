// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#d64700',
    },
    secondary: {
      main: '#333300',
    },
  },
});

export default theme;
